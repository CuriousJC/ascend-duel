*.log

# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore
#
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
# go.work  #I want to include this at least for now

# Scratch notes and analysis, not part of the project
analysis.md
.scratch/

# Screen captures written by internal/trace under -tags debugtrace.
# Anchored with a leading slash: an unanchored "trace/" matches at any depth and would
# swallow internal/trace/, the source package that writes these.
/trace/

# Screen captures written by the scripted demo under -tags demoplay. Anchored for the same
# reason as /trace/ above. These are a *view* of the game rather than art it loads, and a run
# regenerates all of them, so committing them would only ever produce a diff of fifty
# near-identical PNGs.
/demo/

# Card previews written by `go run ./tools/cardsheet`. Nearly forty PNGs plus a page, all
# regenerated on every run — the same argument as /demo/ above, and the reason this does
# *not* follow tools/glyphsheet, which commits its single glyphs.png so GitHub can render
# an image diff in review. If a committed review artefact turns out to be wanted, the
# answer is one composite PNG beside these rather than committing all of them.
#
# Anchored, so a future /internal/cardsheet/ or similar is not swallowed by accident.
/tools/cardsheet/*.png
/tools/cardsheet/index.html
